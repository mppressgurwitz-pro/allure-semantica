# ТЗ для Маши — тест трёх вариантов шаблона `_Advice_SEO_Draft`

**Дата:** 2026-05-20
**От:** Михаил
**Цель:** выбрать рабочий вариант структуры шаблона перед раскаткой на мастер-таблицу и все будущие SKU.

---

## Контекст в двух абзацах

Ты выяснила 2026-05-19, что текущий `_Advice_SEO_Draft` обнуляет «всё остальное» (ТНВЭД, декларации, СГР, габариты) при PUT в WB. Причина:

1. Имена полей в шаблоне (`Пол`, `Тип средства`, `Объём`, `Область нанесения`, `Эффект`, `Активные компоненты`) **не существуют** ни в одной из 14 WB-категорий — это выдуманные имена.
2. Скрипт `uploadSelectedToWB` отправляет в PUT только то, что в шаблоне. WB при PUT заменяет **все** характеристики карточки на то, что пришло; остальное обнуляется.

Из анализа твоих 14 файлов выгрузки WB от 19.05.2026 12:26 получено **35 уникальных WB-характеристик** (не считая базовых полей: артикул, наименование, состав, габариты, сертификаты, ТНВЭД, ИКПУ, НДС). Каждая категория использует только подмножество (от 10 у «Саше» до 17 у «Гели»). У «Пилинга» — 12 полей.

---

## Что я подготовил

В папке `Автоматизация Семантика ВБ` (`C:\Users\mb\Documents\Claude\Projects\Автоматизация Семантика\`):

- `Вариант_A_Union_все_35_полей.xlsx` — один плоский лист со всеми 35 характеристиками + матрица применимости 14 категорий справа.
- `Вариант_B_Категорийный_справочник.xlsx` — два листа: `_WB_Char_Dict` (справочник) + `_Advice_SEO_Draft` с минимальным набором под категорию SKU (для Пилинга — 12 полей).
- `Вариант_C_14_листов_по_категориям.xlsx` — индексный лист `_Index_Categories` + 14 отдельных листов `_Advice_SEO_<Категория>`, в каждом свой набор.

Все три заранее заполнены идентификаторами тестового SKU **Пилинг мужской Press Gurwitz Botanicals PGBOT1M08 (арт 891058906)**.

---

## Что нужно сделать тебе

### Шаг 1. Создать 3 копии тестового файла

В Drive в папке **«Тестовый СКЮ для выгрузки WB»** (https://drive.google.com/drive/folders/14y6ypHhORN75A60cTpKiTGvo9VDG24Gz):

- Открой `тест выгрузки WB— Пиллинг Пресс мужской PGBOT1M08 (арт 891058906)`
- Файл → Создать копию → имя: `тест WB — Вариант A (Union)`. Сохрани в ту же папку.
- Повтори ещё дважды: `тест WB — Вариант B (Категорийный)` и `тест WB — Вариант C (14 листов)`.

### Шаг 2. В каждой копии заменить `_Advice_SEO_Draft` на нужную структуру

В каждой копии:

1. Найди лист `_Advice_SEO_Draft`. Переименуй его в `_Advice_SEO_Draft_OLD_20260520` (на случай отката).
2. Файл → Импорт → Загрузить → выбери соответствующий xlsx из папки `Автоматизация Семантика ВБ`:
   - В копию «Вариант A» → `Вариант_A_Union_все_35_полей.xlsx` (1 лист)
   - В копию «Вариант B» → `Вариант_B_Категорийный_справочник.xlsx` (2 листа: `_WB_Char_Dict` + `_Advice_SEO_Draft`)
   - В копию «Вариант C» → `Вариант_C_14_листов_по_категориям.xlsx` (15 листов: индекс + 14 категорий)
3. В диалоге импорта выбери **«Вставить новые листы»**.
4. Если в варианте A или B импортированный лист пришёл с именем типа `Вариант_A_Union_... — _Advice_SEO_Draft` — переименуй его в `_Advice_SEO_Draft`.

### Шаг 3. Прогнать каждый вариант через свою автоматизацию

Для каждой из трёх копий:

1. Открой `_Advice_SEO_Draft` (или, для варианта C, `_Advice_SEO_Пилинг`).
2. Заполни значения характеристик — возьми их из реального ответа WB API для этого артикула (`GET /content/v2/get/cards/list` по `nmID=891058906`), плюс дополни/поправь под новые SEO-тексты как обычно делаешь.
3. Нажми `previewSelectedToWB` → визуально сверь, что предложено к замене и что предложено оставить как было. Сохрани скриншот.
4. Нажми `uploadSelectedToWB` (на тестовой карточке!). Дождись финиша.
5. Сразу после выгрузки сделай повторный `GET /content/v2/get/cards/list` по тому же `nmID=891058906`.

### Шаг 4. Оценить результат по 4 критериям

Для каждого варианта (A/B/C) запиши:

1. **Все ли SEO-характеристики сохранились с корректными именами?** Имя в WB должно совпадать буквально (например, `Назначение косметического средства`, а не `Назначение`).
2. **Сохранились ли базовые поля (ТНВЭД, ИКПУ, ставка НДС, номер декларации, СГР, габариты, состав, баркод)?** Это главный критерий «рабочести».
3. **Сколько ручных действий потребовалось** при подготовке: нужно ли было что-то вручную править, какие имена не совпали, удобно ли работать с матрицей применимости (вариант A) или 14 листами (вариант C)?
4. **Что сломалось / выдало ошибку**, если что-то сломалось — текст ошибки целиком.

---

## Что прислать в итоге

Короткую сводку:

> Вариант A — статус (рабочий / нет), что сломалось.
> Вариант B — статус, что сломалось.
> Вариант C — статус, что сломалось.
> Какой удобнее заполнять.
> Какой удобнее использовать в скрипте.

Плюс твоя рекомендация — что взять в мастер.

---

## Важное (общее для всех 3 вариантов)

Даже если шаблон с правильными именами — обнуление «остального» может вернуться, если в скрипте `uploadSelectedToWB` перед PUT нет полноценного merge с GET. См. отдельный файл `Masha_WB_API_merge_checklist.md` — там пошагово.

Базовые поля (ТНВЭД, декларации, габариты, состав, баркод) во всех трёх вариантах лежат в красном блоке внизу `_Advice_SEO_Draft`. Скрипт должен подтягивать их из GET и подмешивать в PUT, даже если в шаблоне они пустые.

---

## Деадлайн

Запусти 3 теста, отпишись по результату до конца дня. Если упрёшься во что-то непонятное — пиши Михаилу, не пытайся переделать шаблон сама, иначе мы опять потеряем синхронизацию имён с WB API.
