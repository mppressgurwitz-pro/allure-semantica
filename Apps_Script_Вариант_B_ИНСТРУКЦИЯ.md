# Подмена `_Advice_SEO_Draft` в копии «тест WB — Вариант B»

Скрипт: `Apps_Script_Вариант_B_подмена.gs` (рядом с этим файлом).

Что он делает за один запуск:
1. Открывает копию **«тест WB — Вариант B (Категорийный справочник)»** (ID `1NOO50oa9CWfXzA7AixR7zapj1xzr2e3xhK7lM6Cc3bQ` в папке «Тестовый СКЮ для выгрузки WB»).
2. Находит лист `_Advice_SEO_Draft` и переименовывает в `_Advice_SEO_Draft_OLD_20260520` (для отката).
3. Создаёт два новых листа: `_WB_Char_Dict` (39 × 18) и `_Advice_SEO_Draft` (47 × 3) с данными из `Вариант_B_Категорийный_справочник.xlsx`.

Остальные листы копии (Инструкция, _Advice_Last_Raw, скрипт `uploadSelectedToWB` и т. д.) **не трогаются** — Apps Script работает только с двумя названными листами.

---

## Как запустить (≈ 90 секунд)

1. Перейдите на https://script.google.com/ → **«Новый проект»**.
2. Полностью удалите дефолтный код в файле `Код.gs`.
3. Откройте `Apps_Script_Вариант_B_подмена.gs` любым редактором (или просто двойным кликом) → выделите всё содержимое (Ctrl+A) → скопируйте (Ctrl+C) → вставьте в `Код.gs`.
4. Сохраните проект (Ctrl+S) — название любое, например `WB-Вариант-B-Подмена`.
5. Вверху редактора в выпадающем списке функций выберите `main` → нажмите кнопку **Run** (▶).
6. При первом запуске Google попросит разрешения: **«Просмотр и управление вашими таблицами»** — выдайте.
7. Внизу откроется панель «Журнал выполнения». Должно быть примерно так:
   ```
   Открыта таблица: тест WB — Вариант B (Категорийный справочник)
   Переименован _Advice_SEO_Draft -> _Advice_SEO_Draft_OLD_20260520
   Создан лист _WB_Char_Dict
   Заполнен _WB_Char_Dict: 39 x 18
   Создан лист _Advice_SEO_Draft
   Заполнен _Advice_SEO_Draft: 47 x 3
   Готово.
   ```

---

## Проверка после запуска

Откройте https://docs.google.com/spreadsheets/d/1NOO50oa9CWfXzA7AixR7zapj1xzr2e3xhK7lM6Cc3bQ/edit и убедитесь, что:

- Внизу есть три листа: `_Advice_SEO_Draft_OLD_20260520` (архив), `_WB_Char_Dict` (новый), `_Advice_SEO_Draft` (новый).
- `_Advice_SEO_Draft` начинается со строк «Артикул WB / 891058906», «Категория WB / Пилинг» и содержит 12 строк WB-характеристик для категории Пилинг (Вид пилинга, Действие, Комплектация, Назначение косметического средства, Объем товара, Особенности косметики, Срок годности, Страна производства, Торговое наименование, Упаковка, Форма упаковки, Формат пилинга).
- `_WB_Char_Dict` — это таблица 35 характеристик × 14 категорий с пометками 0/1, в столбце «Пилинг» проставлены 1 ровно для 12 строк выше.

Если что-то пошло не так — откройте `_Advice_SEO_Draft_OLD_20260520`, переименуйте обратно в `_Advice_SEO_Draft`, удалите два новых листа — состояние вернётся в исходное.

---

## Идемпотентность

Скрипт можно запускать повторно — он не уничтожит архивный `_OLD_20260520` и не дублирует листы. При втором запуске просто перезапишет содержимое `_WB_Char_Dict` и `_Advice_SEO_Draft`.

---

## Что дальше (после подтверждения теста на этой копии)

Когда вы прогоните этот SKU через `previewSelectedToWB` / `uploadSelectedToWB` и убедитесь, что ТНВЭД/декларации/габариты НЕ обнуляются (см. `Masha_TZ_тест_3_вариантов_шаблона.md`, шаг 4) — этот же шаблон (структура B) тиражируем на остальные SKU. Для каждой следующей категории `_Advice_SEO_Draft` будет содержать своё подмножество полей из `_WB_Char_Dict` — по той же логике, но с другим набором характеристик.
